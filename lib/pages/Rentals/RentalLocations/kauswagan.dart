import 'package:flutter/material.dart';

class KauswaganPage extends StatelessWidget {
  const KauswaganPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bulua Location"),
      ),
    );
  }
}