import 'package:flutter/material.dart';

class LapasanPage extends StatelessWidget {
  const LapasanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bulua Location"),
      ),
    );
  }
}