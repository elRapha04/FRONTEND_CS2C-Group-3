import 'package:flutter/material.dart';

class CarmenPage extends StatelessWidget {
  const CarmenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bulua Location"),
      ),
    );
  }
}