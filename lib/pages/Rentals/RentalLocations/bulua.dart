import 'package:flutter/material.dart';

class BuluaPage extends StatelessWidget {
  const BuluaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bulua Location"),
      ),
    );
  }
}
